# -*- coding: utf-8 -*-
interval_var_inter = []

children = []

permutation = [ 0 ]

interpolation['nodal'] = val[0]
interpolation['bubble'] = val[0]
interpolation["der_nodal"] = val[0]

quality = number(1)
degree = 0

# ---------------------------------------------------------------------------------
def local_coordinates(point):
  return vector([])
def pos_using_local_coordinates(coords):
  return positions[0]

# ---------------------------------------------------------------------------------
gauss_points[100] = [
  ( 1.0, { var_inter[0] : number(0) } ),
]
