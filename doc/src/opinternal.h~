#ifndef OP_INTERNAL_H
#define OP_INTERNAL_H

#include <map>
#include <string>

//#include "pagecomment.h"
#include "op.h"

struct PageComment ;
struct VisitorBloc_NameBaseClass ;
struct ListTarget ;



struct OpInternal : Op {
    OpInternal( ListTarget* pllink, map<string,string>* pt  ) { ptr_list_target = pllink ; ptr_tree = pt ; }
    virtual ~OpInternal() {}
    virtual void operator()( PageComment* page ) ;
};

#endif

